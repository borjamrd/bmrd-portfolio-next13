import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Projects",
  description: "text",
};

const page = ({}) => {
  return <div>projects works</div>;
};

export default page;
